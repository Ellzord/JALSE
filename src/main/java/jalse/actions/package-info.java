/**
 * This package is for scheduling and managing {@link jalse.actions.Action} (like
 * {@link java.lang.Runnable} but with a {@link jalse.actions.ActionContext} and potentially
 * associated actor). Under this package there are different implementations of
 * {@link jalse.actions.ActionEngine} (which can schedule actions).
 *
 * @see jalse.actions.ForkJoinActionEngine
 * @see jalse.actions.ThreadPoolActionEngine
 * @see jalse.actions.ManualActionEngine
 * @see jalse.actions.Actions
 */
package jalse.actions;

