version = '1.0.3'

apply plugin: 'java'
apply plugin: 'eclipse'

jar {
    baseName = 'JALSE'
    manifest {
        attributes 'Implementation-Title': baseName,
                   'Implementation-Version': version,
                   'Implementation-Vendor': 'Elliot Ford (Ellzord)'
    }
}
